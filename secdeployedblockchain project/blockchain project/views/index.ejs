<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SecureChain | Decentralized Asset Recovery</title>
    <meta name="description"
        content="SecureChain provides decentralized asset recovery for the blockchain era. Protect your digital assets with military-grade security.">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link
        href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800;900&family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="/css/index.css">
    <style>
        /* Extra structural styles for the new layout just in case */
        .scroller-container {
            width: 100%;
            overflow: hidden;
        }

        /* Interactive grid background */
        #interactiveGrid {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -3;
            background-size: 50px 50px;
            background-image:
                linear-gradient(to right, rgba(0, 240, 255, 0.05) 1px, transparent 1px),
                linear-gradient(to bottom, rgba(0, 240, 255, 0.05) 1px, transparent 1px);
            transform: perspective(500px) rotateX(60deg) translateY(-100px) translateZ(-200px);
            animation: gridMove 20s linear infinite;
        }

        @keyframes gridMove {
            0% {
                background-position: 0 0;
            }

            100% {
                background-position: 50px 50px;
            }
        }

        /* Footer styling for index mapping */
        footer {
            background: #050B14;
            border-top: 1px solid rgba(0, 240, 255, 0.1);
            padding: 80px 0 30px;
            margin-top: 100px;
            font-family: 'Plus Jakarta Sans', sans-serif;
            box-shadow: 0 -10px 40px rgba(0, 0, 0, 0.5);
        }

        .footer-content {
            display: grid;
            grid-template-columns: 2fr 1fr 1fr 1fr;
            gap: 48px;
            margin-bottom: 60px;
        }

        .footer-logo {
            font-family: 'Outfit';
            font-size: 1.5rem;
            font-weight: 800;
            margin-bottom: 16px;
            display: inline-block;
            background: var(--gradient-1);
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .text-gradient {
            background: var(--gradient-1);
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
            display: inline-block;
        }

        .footer-about p {
            color: var(--text-muted);
            font-size: 0.95rem;
        }

        .footer-social {
            display: flex;
            gap: 12px;
            margin-top: 24px;
        }

        .footer-social a {
            width: 40px;
            height: 40px;
            border-radius: 12px;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--text-muted);
            font-size: 1.1rem;
            transition: all 0.3s ease;
        }

        .footer-social a:hover {
            background: var(--gradient-1);
            border-color: transparent;
            transform: translateY(-3px);
            color: #050B14;
            box-shadow: 0 5px 15px rgba(0, 240, 255, 0.3);
        }

        h3.footer-title {
            font-family: 'Outfit';
            font-size: 1.1rem;
            margin-bottom: 24px;
            color: var(--text-main);
        }

        .footer-links {
            list-style: none;
        }

        .footer-links li {
            margin-bottom: 12px;
        }

        .footer-links a {
            color: var(--text-muted);
            text-decoration: none;
            transition: color 0.3s;
            font-size: 0.95rem;
        }

        .footer-links a:hover {
            color: var(--primary);
        }

        .footer-bottom {
            padding-top: 30px;
            border-top: 1px solid rgba(255, 255, 255, 0.05);
            text-align: center;
            color: var(--text-muted);
            font-size: 0.9rem;
        }

        /* Responsive Styles */
        @media (max-width: 1024px) {
            .footer-content {
                grid-template-columns: 1fr 1fr;
            }
        }

        @media (max-width: 768px) {
            .footer-content {
                grid-template-columns: 1fr;
                text-align: center;
                gap: 40px;
            }

            .footer-social {
                justify-content: center;
            }

            .nav-actions {
                display: none;
            }
        }
    </style>
    <link rel="stylesheet" href="/css/page-transition.css">
</head>

<body>
    <!-- Background Effects -->
    <div id="interactiveGrid"></div>
    <div id="mouseGlow"
        style="position: fixed; width: 400px; height: 400px; border-radius: 50%; pointer-events: none; background: radial-gradient(circle, rgba(0, 240, 255, 0.15) 0%, transparent 70%); transform: translate(-50%, -50%); z-index: -1; transition: opacity 0.3s; opacity: 0;">
    </div>
    <canvas id="particleCanvas"
        style="position: fixed; top: 0; left: 0; width: 100%; height: 100%; z-index: -2; pointer-events: none; opacity: 0.8;"></canvas>
    <div class="ambient-bg">
        <div class="orb orb-1"></div>
        <div class="orb orb-2"></div>
        <div class="orb orb-3"></div>
    </div>

    <!-- Floating Navbar -->
    <script>
        function closeNav() {
            var n = document.querySelector('.nav-links');
            if (n) { n.classList.remove('active'); n.style.left = '-100vw'; }
        }
        function toggleNav() {
            var n = document.querySelector('.nav-links');
            if (n) { n.style.left = ''; n.classList.toggle('active'); }
        }
    </script>
    <header id="mainHeader" class="floating-nav">
        <div class="nav-container">
            <a href="/" class="logo">
                <div class="logo-icon">
                    <i class="fas fa-shield-alt"></i>
                </div>
                <div class="logo-text">SecureChain</div>
            </a>

            <button class="mobile-menu-btn" onclick="toggleNav()">
                <i class="fas fa-bars"></i>
            </button>

            <nav class="nav-links">
                <!-- Mobile Navbar Header -->
                <div class="mobile-menu-header mobile-only">
                    <a href="/" class="logo">
                        <div class="logo-icon">
                            <i class="fas fa-shield-alt"></i>
                        </div>
                        <div class="logo-text">SecureChain</div>
                    </a>
                    <button class="close-menu-btn" onclick="closeNav()">
                        <i class="fas fa-times"></i>
                    </button>
                </div>

                <a href="/#features" class="nav-link" onclick="closeNav()"><i class="fas fa-layer-group mobile-nav-icon"></i> Features</a>
                <a href="/user/about" class="nav-link" onclick="closeNav()"><i class="fas fa-users mobile-nav-icon"></i> About Us</a>
                <a href="/#how-it-works" class="nav-link" onclick="closeNav()"><i class="fas fa-cogs mobile-nav-icon"></i> How it Works</a>
                <a href="/user/login" class="nav-link mobile-only" onclick="closeNav()"><i class="fas fa-sign-in-alt mobile-nav-icon"></i>
                    Sign In</a>
                <a href="/user/register" class="btn btn-primary mobile-only" onclick="closeNav()">
                    <i class="fas fa-user-plus" style="margin-right: 8px;"></i> Create Account
                </a>
            </nav>
            <div class="nav-overlay" onclick="closeNav()"></div>

            <div class="nav-actions">
                <a href="/user/login" class="nav-link" style="margin-right: 12px; border:none; padding:12px; height:auto;">Sign In</a>
                <a href="/user/register" class="btn-split">
                    <span class="split-text">Get Started</span>
                    <span class="split-icon"><i class="fas fa-plus"></i></span>
                </a>
            </div>
        </div>
    </header>

    <!-- Main Hero Section -->
    <section class="hero container">
        <div class="hero-grid">
            <div class="hero-content">
                <div class="badge">
                    <i class="fas fa-rocket"></i> Web3 Asset Protection
                </div>
                <h1 class="block-reveal-wrapper hero-block-reveal"><span class="block-reveal-text">Your Digital Legacy,<br> <span class="text-gradient" style="-webkit-text-fill-color:transparent;">Fully Secured.</span></span></h1>
                <p>Don't let your cryptocurrency disappear. SecureChain allows you to set up decentralized, trustless
                    asset recovery protocols ensuring your wealth reaches the right hands.</p>
                <div class="hero-buttons">
                    <a href="/user/register" class="btn btn-primary pulse-btn">
                        <i class="fas fa-wallet"></i> Connect Wallet
                    </a>
                    <a href="#features" class="btn btn-outline glow-btn">
                        <i class="fas fa-play"></i> Watch Demo
                    </a>
                </div>
            </div>

            <div class="hero-visual">
                <!-- Floating Glass Panel -->
                <div class="glass-panel">
                    <!-- CSS Dashboard Preview -->
                    <div class="mock-dash">
                        <div class="mock-header">
                            <div class="mock-dots"><span></span><span></span><span></span></div>
                            <div class="mock-title">Vault Interface</div>
                        </div>
                        <div class="mock-body">
                            <div class="mock-balance">
                                <span
                                    style="font-size: 0.85rem; color: var(--text-muted); text-transform: uppercase; letter-spacing: 1px; font-weight: 600;">Total
                                    Protected Value</span>
                                <h2
                                    style="font-family: 'Outfit'; font-size: 2.6rem; margin: 5px 0 15px 0; color: var(--text-main); letter-spacing: -1px;">
                                    $2,458,190.00</h2>
                            </div>
                            <div style="display: flex; gap: 12px; margin-bottom: 25px;">
                                <div style="flex: 1; background: rgba(255,255,255,0.03); border-radius: 12px; padding: 15px; border: 1px solid rgba(255,255,255,0.05); box-shadow: 0 4px 15px rgba(0,0,0,0.1); transition: transform 0.3s; cursor: pointer;"
                                    onmouseover="this.style.transform='translateY(-3px)'"
                                    onmouseout="this.style.transform='translateY(0)'">
                                    <div
                                        style="font-size: 0.8rem; color: var(--text-muted); margin-bottom: 5px; display: flex; align-items: center; justify-content: space-between;">
                                        Ethereum <i class="fab fa-ethereum" style="color:var(--secondary)"></i></div>
                                    <div
                                        style="font-weight: 800; font-family: 'Outfit'; font-size: 1.2rem; color: var(--text-main);">
                                        142.5 ETH</div>
                                </div>
                                <div style="flex: 1; background: rgba(255,255,255,0.03); border-radius: 12px; padding: 15px; border: 1px solid rgba(255,255,255,0.05); box-shadow: 0 4px 15px rgba(0,0,0,0.1); transition: transform 0.3s; cursor: pointer;"
                                    onmouseover="this.style.transform='translateY(-3px)'"
                                    onmouseout="this.style.transform='translateY(0)'">
                                    <div
                                        style="font-size: 0.8rem; color: var(--text-muted); margin-bottom: 5px; display: flex; align-items: center; justify-content: space-between;">
                                        Bitcoin <i class="fab fa-bitcoin" style="color:#f7931a"></i></div>
                                    <div
                                        style="font-weight: 800; font-family: 'Outfit'; font-size: 1.2rem; color: var(--text-main);">
                                        3.8 BTC</div>
                                </div>
                            </div>
                            <div class="mock-bar">
                                <div class="mock-progress"></div>
                            </div>
                            <div
                                style="display: flex; justify-content: space-between; font-size: 0.8rem; color: var(--text-muted); margin-top: 12px; font-weight: 600;">
                                <span>Status: <span style="color: #10b981;">Active
                                        Monitoring</span></span>
                                <span>Checked In: Today</span>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Floating Elements around the panel -->
                <div class="visual-element ve-1">
                    <div class="icon"><i class="fab fa-ethereum"></i></div>
                    <div>
                        <div style="font-weight:700; font-family:'Outfit'; font-size: 1.1rem; color: var(--text-main);">
                            Recovery
                            Active</div>
                        <div style="font-size:0.85rem; color:var(--text-muted);">Smart Contract v2.1</div>
                    </div>
                </div>

                <div class="visual-element ve-2">
                    <div class="icon-lock"><i class="fas fa-shield-alt"></i></div>
                    <div>
                        <div style="font-weight:700; font-family:'Outfit'; font-size: 1.1rem; color: var(--text-main);">
                            Military
                            Grade</div>
                        <div style="font-size:0.85rem; color:var(--text-muted)">AES-256 Encryption</div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Infinite Scroll Trust Banner -->
    <div class="scroller-container">
        <div class="trust-banner">
            <div class="trust-text">Securing Assets Across Web3</div>
            <div style="overflow: hidden; width: 100%;">
                <div class="scroller">
                    <div class="scroller-item"><i class="fab fa-ethereum"></i> Ethereum</div>
                    <div class="scroller-item"><i class="fab fa-bitcoin"></i> Bitcoin</div>
                    <div class="scroller-item"><i class="fas fa-cube"></i> Polygon</div>
                    <div class="scroller-item"><i class="fas fa-link"></i> Smart Contracts</div>
                    <div class="scroller-item"><i class="fas fa-shield-alt"></i> DeFi Protocols</div>
                    <!-- Duplicate for infinite scroll -->
                    <div class="scroller-item"><i class="fab fa-ethereum"></i> Ethereum</div>
                    <div class="scroller-item"><i class="fab fa-bitcoin"></i> Bitcoin</div>
                    <div class="scroller-item"><i class="fas fa-cube"></i> Polygon</div>
                    <div class="scroller-item"><i class="fas fa-link"></i> Smart Contracts</div>
                    <div class="scroller-item"><i class="fas fa-shield-alt"></i> DeFi Protocols</div>
                </div>
            </div>
        </div>
    </div>

    <!-- Stats Section -->
    <section class="stats-section container">
        <div class="stats-grid">
            <div class="stat-item">
                <i class="fas fa-shield-alt stat-icon"></i>
                <h3 class="stat-number">250K+</h3>
                <p class="stat-label">Wallets Secured</p>
            </div>
            <div class="stat-item">
                <i class="fas fa-chart-line stat-icon"></i>
                <h3 class="stat-number">$1.2B+</h3>
                <p class="stat-label">Assets Protected</p>
            </div>
            <div class="stat-item">
                <i class="fas fa-server stat-icon"></i>
                <h3 class="stat-number">100%</h3>
                <p class="stat-label">Uptime Execution</p>
            </div>
            <div class="stat-item">
                <i class="fas fa-link stat-icon"></i>
                <h3 class="stat-number">12+</h3>
                <p class="stat-label">Chains Supported</p>
            </div>
        </div>
    </section>

    <!-- How It Works Section -->
    <section class="how-it-works container" id="how-it-works">
        <div class="section-header">
            <div class="mask-wrapper" style="overflow:visible; display:block; margin-bottom:20px;">
                <h2 class="block-reveal-wrapper"><span class="block-reveal-text">Three Steps to Security</span></h2>
            </div>
            <div class="mask-wrapper" style="display:block;">
                <p class="mask-reveal delay-1">Setting up your decentralized asset recovery is incredibly simple and fully non-custodial.</p>
            </div>
        </div>

        <div class="steps-container">
            <div class="step-card">
                <div class="step-number">01</div>
                <h3>Connect Wallet</h3>
                <p>Link your Web3 wallet securely. We never ask for your private keys; our smart contracts act as
                    conditional observers.</p>
                <div class="step-visual">
                    <i class="fas fa-wallet"
                        style="font-size: 3rem; color: var(--primary); filter: drop-shadow(0 10px 15px rgba(79, 70, 229, 0.2));"></i>
                </div>
            </div>
            <div class="step-arrow"><i class="fas fa-chevron-right"></i></div>
            <div class="step-card">
                <div class="step-number">02</div>
                <h3>Set Recovery Rules</h3>
                <p>Define your trusted beneficiaries, configure the inactivity timer (e.g., 6 months), and set the
                    distribution percentages.</p>
                <div class="step-visual">
                    <i class="fas fa-sliders-h"
                        style="font-size: 3rem; color: var(--secondary); filter: drop-shadow(0 10px 15px rgba(14, 165, 233, 0.2));"></i>
                </div>
            </div>
            <div class="step-arrow"><i class="fas fa-chevron-right"></i></div>
            <div class="step-card">
                <div class="step-number">03</div>
                <h3>Automated Protection</h3>
                <p>If you fail to trigger the basic "Check In" within your set timeframe, smart contracts will
                    autonomously execute the transfer.</p>
                <div class="step-visual">
                    <i class="fas fa-robot"
                        style="font-size: 3rem; color: var(--accent); filter: drop-shadow(0 10px 15px rgba(244, 63, 94, 0.2));"></i>
                </div>
            </div>
        </div>
    </section>

    <!-- Core Features Section -->
    <section class="features container" id="features">
        <div class="section-header">
            <div class="mask-wrapper" style="overflow:visible; display:block; margin-bottom:20px;">
                <h2 class="block-reveal-wrapper br-delay-1"><span class="block-reveal-text">Next-Gen Recovery</span></h2>
            </div>
            <div class="mask-wrapper" style="display:block;">
                <p class="mask-reveal delay-1">We've combined the latest in blockchain technology with intuitive design to give you ultimate peace of
                mind.</p>
            </div>
        </div>

        <div class="features-grid stagger-children">
            <div class="feature-box">
                <div class="fb-icon"><i class="fas fa-fingerprint"></i></div>
                <h3>Decentralized Identity</h3>
                <p>Your assets are tied to your cryptographic identity. Setup automated transfer rules directly to your
                    beneficiaries' wallets.</p>
            </div>

            <div class="feature-box">
                <div class="fb-icon"><i class="fas fa-clock"></i></div>
                <h3>Dead Man's Switch</h3>
                <p>If you don't check in within a defined time period, smart contracts automatically initiate your
                    custom asset recovery flow.</p>
            </div>

            <div class="feature-box">
                <div class="fb-icon"><i class="fas fa-network-wired"></i></div>
                <h3>Cross-Chain Compatible</h3>
                <p>Whether you hold assets on Ethereum, Binance Smart Chain, or Polygon, SecureChain can manage and
                    recover them all.</p>
            </div>

            <div class="feature-box">
                <div class="fb-icon"><i class="fas fa-user-shield"></i></div>
                <h3>Zero-Knowledge</h3>
                <p>We never see your private keys. Our cryptographic wrapper ensures your credentials stay encrypted
                    client-side.</p>
            </div>

            <div class="feature-box">
                <div class="fb-icon"><i class="fas fa-file-contract"></i></div>
                <h3>Audited Contracts</h3>
                <p>Our smart contracts have been thoroughly vetted by top security firms to ensure zero vulnerabilities.
                </p>
            </div>

            <div class="feature-box">
                <div class="fb-icon"><i class="fas fa-bolt"></i></div>
                <h3>Instant Execution</h3>
                <p>Once conditions are met, transfers happen instantly at the speed of the blockchain with no middleman
                    delays.</p>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="container">
            <div class="footer-content">
                <div>
                    <div class="footer-logo">SecureChain</div>
                    <div class="footer-about">
                        <p>Decentralized asset recovery for the blockchain era. Ensuring your digital wealth is never
                            lost.</p>
                    </div>
                    <div class="footer-social">
                        <a href="#"><i class="fab fa-twitter"></i></a>
                        <a href="#"><i class="fab fa-discord"></i></a>
                        <a href="#"><i class="fab fa-github"></i></a>
                    </div>
                </div>

                <div>
                    <h3 class="footer-title">Platform</h3>
                    <ul class="footer-links">
                        <li><a href="#">How it Works</a></li>
                        <li><a href="#features">Features</a></li>
                        <li><a href="#">Security Audits</a></li>
                        <li><a href="#">Supported Protocols</a></li>
                    </ul>
                </div>

                <div>
                    <h3 class="footer-title">Developers</h3>
                    <ul class="footer-links">
                        <li><a href="#">Documentation</a></li>
                        <li><a href="#">API Reference</a></li>
                        <li><a href="#">Smart Contracts</a></li>
                        <li><a href="#">Bug Bounty</a></li>
                    </ul>
                </div>

                <div>
                    <h3 class="footer-title">Company</h3>
                    <ul class="footer-links">
                        <li><a href="/user/about">About Us</a></li>
                        <li><a href="#">Careers</a></li>
                        <li><a href="#">Privacy Policy</a></li>
                        <li><a href="#">Terms of Service</a></li>
                    </ul>
                </div>
            </div>

            <div class="footer-bottom">
                &copy; 2025 SecureChain Global. All Rights Reserved. <br>
                <span style="display:inline-block; margin-top: 10px; color: var(--primary);">Smart Contract:
                    0x89205A3A3b2A69De6Dbf7f01ED13B2108B2c43e7</span>
            </div>
        </div>
    </footer>

    <script>
        // Header scroll effect
        const header = document.getElementById('mainHeader');
        window.addEventListener('scroll', () => {
            if (window.scrollY > 50) {
                header.classList.add('scrolled');
            } else {
                header.classList.remove('scrolled');
            }
        });

        // 3D Tilt effect for Hero Visual
        const heroVisual = document.querySelector('.hero-visual');
        const glassPanel = document.querySelector('.glass-panel');

        if (heroVisual && glassPanel) {
            heroVisual.addEventListener('mousemove', (e) => {
                const rect = heroVisual.getBoundingClientRect();
                const x = e.clientX - rect.left;
                const y = e.clientY - rect.top;

                const centerX = rect.width / 2;
                const centerY = rect.height / 2;

                const rotateX = ((y - centerY) / centerY) * -10;
                const rotateY = ((x - centerX) / centerX) * 10;

                glassPanel.style.transform = `perspective(1000px) rotateX(${rotateX}deg) rotateY(${rotateY}deg)`;
            });

            heroVisual.addEventListener('mouseleave', () => {
                glassPanel.style.transform = `perspective(1000px) rotateY(-5deg) rotateX(5deg)`;
            });
        }

        // Mouse glow effect
        const mouseGlow = document.getElementById('mouseGlow');
        document.addEventListener('mousemove', (e) => {
            mouseGlow.style.opacity = '1';
            mouseGlow.style.left = e.clientX + 'px';
            mouseGlow.style.top = e.clientY + 'px';
        });

        document.addEventListener('mouseleave', () => {
            mouseGlow.style.opacity = '0';
        });

        // Simple Particle Setup
        const canvas = document.getElementById('particleCanvas');
        const ctx = canvas.getContext('2d');
        let width, height;
        let particles = [];

        function resize() {
            width = window.innerWidth;
            height = window.innerHeight;
            canvas.width = width;
            canvas.height = height;
        }

        window.addEventListener('resize', resize);
        resize();

        class Particle {
            constructor() {
                this.x = Math.random() * width;
                this.y = Math.random() * height;
                this.size = Math.random() * 2 + 0.5;
                this.speedX = (Math.random() - 0.5) * 0.8;
                this.speedY = (Math.random() - 0.5) * 0.8;
                this.opacity = Math.random() * 0.5 + 0.2;
                this.color = Math.random() > 0.5 ? '0, 240, 255' : '58, 134, 255';
            }
            update() {
                this.x += this.speedX;
                this.y += this.speedY;

                if (this.x > width) this.x = 0;
                if (this.x < 0) this.x = width;
                if (this.y > height) this.y = 0;
                if (this.y < 0) this.y = height;
            }
            draw() {
                ctx.fillStyle = `rgba(${this.color}, ${this.opacity})`;
                ctx.beginPath();
                ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
                ctx.fill();
            }
        }

        for (let i = 0; i < 70; i++) {
            particles.push(new Particle());
        }

        function animate() {
            ctx.clearRect(0, 0, width, height);

            for (let i = 0; i < particles.length; i++) {
                particles[i].update();
                particles[i].draw();
            }
            requestAnimationFrame(animate);
        }
        animate();

        // Block reveal & mask reveal scroll observer
        const revealObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    // Trigger block-reveal
                    entry.target.querySelectorAll('.block-reveal-wrapper').forEach(el => {
                        el.classList.add('animate-in');
                    });
                    // Trigger mask-reveal
                    entry.target.querySelectorAll('.mask-reveal').forEach(el => {
                        el.classList.add('visible');
                    });
                    // Trigger stagger-children
                    if (entry.target.classList.contains('stagger-children')) {
                        entry.target.classList.add('visible');
                    }
                    // Trigger section-line-reveal
                    if (entry.target.classList.contains('section-line-reveal')) {
                        entry.target.classList.add('visible');
                    }
                    revealObserver.unobserve(entry.target);
                }
            });
        }, { threshold: 0.15 });

        // Observe all section-headers, stagger-children, and section-line-reveal elements
        document.querySelectorAll('.section-header, .stagger-children, .section-line-reveal').forEach(el => {
            revealObserver.observe(el);
        });
    </script>
    <script src="/js/page-transition.js"></script>
</body>

</html>